allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
overrides:
  # @better-auth/kysely-adapter 1.6.12 imports DEFAULT_MIGRATION_TABLE which
  # kysely 0.29.x dropped; pin to 0.28.x (still within Better Auth's range).
  kysely: 0.28.17
